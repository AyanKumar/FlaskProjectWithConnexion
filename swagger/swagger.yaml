# generated by ps_gen
swagger: "2.0"

info:
  title: simple calculator
  description: API for adding two numbers
  version: "1"

basePath: /add/api/v1



responses:
  BadRequest:
    description: Bad Request
    schema:
      $ref: "#/definitions/Problem"
  Created:
    description: Created
  NoContent:
    description: No Content
  NotFound:
    description: Not Found
    schema:
      $ref: "#/definitions/Problem"
  Unauthorized:
    description: Unauthorized
    schema:
      $ref: "#/definitions/Problem"

paths:

  /number1/{number1}/number2/{number2}:
    get:
      description: Use this api to add two numbers
      summary: Add Numbers
      operationId: calculate.add_numbers
      parameters:
        - name: number1
          in: path
          description: First number
          required: true
          type: string
        - name: number2
          in: path
          description: Second number
          required: true
          type: string
      responses:
        200:
          description: some message
        403:
          $ref: "#/responses/Unauthorized"
        404:
          $ref: "#/responses/NotFound"


definitions:

  # Schema for error response body

  Problem:
    type: object
    properties:
      detail:
        type: string
      status:
        type: number
      title:
        type: string
      type:
        type: string
    required: ["detail", "status", "title", "type"]



